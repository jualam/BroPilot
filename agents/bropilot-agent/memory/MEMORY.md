# BroPilot Memory

- Use `python -m pytest` for the demo repo.
- BroPilot disables Gitclaw cli tool through the SDK because Windows shell commands were unreliable.
- Prefer read/write tools and backend verification.
- Keep edits small enough for a human reviewer to audit quickly.
- Do not commit or push from an agent run.
